# PassoFlow ロードマップ

更新日: 2026-09-06。現在のバージョン: **0.1.1**。

## 製品の方向性

PassoFlow は個人用・ローカル実行の自動化ツールです。評価対象は GUI 操作、画像認識、シナリオ作成の容易さ、再実行性、安全性、使いやすさに集中します。

オーケストレーション、深いデータワークフロー、分散実行、クラウド運用、チーム管理、監査基盤は優先しません。

## 現在の基盤

- [x] パレット、キャンバス、パラメータパネル、検証、Undo/Redo、ループ、分岐、テーブルプレビュー、実行ログを備えたブラウザエディタ。
- [x] 信頼度、リトライ、オフセット、候補ログ、任意の `region` 検索範囲に対応した画像操作。
- [x] クリック前の赤丸表示を標準 0.25 秒に短縮（安全確認のため表示は維持し、設定で変更可能）。
- [x] 画面画像で既定ブラウザを操作する方式: `open_url`、`activate_window`、`click_image`、`type_text`。
- [x] Playwright と CSS セレクタで別ブラウザを操作する方式: `browser_navigate`、`browser_click`、`browser_fill`、`browser_wait_for`。
- [x] ローカル検証、安全チェック、シナリオ保存、明示的な実行・停止操作。
- [x] 利用者向け変更に対応した英語・日本語・中国語の UI 文字列。

## 次の優先事項

### 1. 最初のフローを作りやすくする

- [ ] 「アクション追加」から「検証して実行」までの初回導線を短くする。
- [ ] セットアップ手順を改善し、不足依存関係やブラウザの問題を画面内で説明する。
- [ ] 初回成功までの時間と、失敗ステップからの復旧しやすさを測る。

### 2. 画像認識と GUI 操作の信頼性を高める

- [ ] 画像候補の追加・削除・並べ替えをエディタから行えるようにする。
- [ ] アクティブウィンドウ基準の `region` と、座標単位の説明を追加する。
- [ ] DPI・表示倍率・マルチスケール照合への案内を改善する。
- [ ] 前後スクリーンショットと、より安全なクリック確認を追加する。

### 3. Web 操作を改善する

- [ ] セレクタ取得と簡易プレビュー・テストを追加する。
- [ ] 画面画像方式と DOM 方式の違いを保ったまま、ブラウザセッション設定を提供する。
- [ ] セレクタのタイムアウト、要素切り離し、ブラウザ起動失敗を解決しやすくする。

### 4. 再利用性とアクセシビリティを改善する

- [ ] よく使うパラメータ構成をコピー・編集しやすくする。
- [ ] キーボード操作、フォーカス、ローカライズ、色だけに依存しない状態表示を回帰テストで維持する。

## 完了条件

実装、検証動作、利用者向け文書、3 言語の UI が一致し、build/lint/smoke と影響範囲のブラウザ確認を通過し、安全性に関わる動作が UI 内で説明されていることを完了条件とします。
